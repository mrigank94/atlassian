export const getFeatureState = (flagName, defaultValue=false) => {

    //Date.now()
    //Data => timestamp
    if(!data[flagName]) {
        return false;
    }
    return new Promise((resolve, reject) => {
        reject('Service is down')
    });
}